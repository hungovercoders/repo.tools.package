# Useful Commands

```bash
python --version
uv --version
mkdocs gh-deploy
mkdocs serve
git-cliff -c cliff.toml
uv add requests
uv remove requests
uv lock --upgrade-package requests
uv build
uv init --lib my-package
pip install -e .
uvx ruff check --fix
```
